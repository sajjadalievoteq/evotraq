/// Feature toggles for industry-specific integrations.
///
/// Set [kTobaccoExtensionEnabled] to `true` to restore GTIN / GLN / SSCC tobacco
/// extension widgets and API fetch/save paths.

/// Tobacco extension (GTIN/GLN/SSCC UI + remote fetch/persist). Disabled until product-ready.
const bool kTobaccoExtensionEnabled = false;
