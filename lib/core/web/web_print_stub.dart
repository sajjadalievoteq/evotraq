Future<void> printImageBytes({
  required List<int> bytes,
  String mimeType = 'image/png',
  String title = 'Barcode',
}) async {
  throw UnsupportedError('printImageBytes is not supported on this platform');
}
